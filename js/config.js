/**
 * config.js
 * Aquí guardamos toda la info importante pa que el juego sepa qué onda.
 * Para cambiar la velocidad o el tamaño.
 */

export const CONFIG = {
    CANVAS_WIDTH: 400,
    CANVAS_HEIGHT: 500,
    FPS: 60,
    PLAYER_SPEED: 4.5,
    FOCUS_SPEED: 2.0,
    PLAYER_SIZE: 5,
    BOSS_SIZE: 30,
    BULLET_Pool_SIZE: 3000,
    SURVIVAL_TIME: 29.0,
};
